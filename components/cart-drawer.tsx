"use client";import Link from "next/link";import {useStore} from "./store-provider";export function CartDrawer(){const {cart,change,openCart,setOpenCart}=useStore();const total=cart.reduce((n,x)=>n+(x.product.salePrice??x.product.price)*x.quantity,0);return <aside className={openCart?"drawer open":"drawer"}><button className="close" onClick={()=>setOpenCart(false)}>×</button><span className="eyebrow">Your selection</span><h2>Shopping bag</h2>{!cart.length?<p className="empty">Your bag is waiting for something exceptional.</p>:<>{<div className="lines">{cart.map(({product,quantity})=><div className="line" key={product.id}><img src={product.image} alt=""/><div><b>{product.name}</b><small>{product.brand}</small><span>${(product.salePrice??product.price).toFixed(2)}</span></div><div className="quantity"><button onClick={()=>change(product.id,quantity-1)}>−</button>{quantity}<button onClick={()=>change(product.id,quantity+1)}>+</button></div></div>)}</div><div className="total"><span>Subtotal</span><b>${total.toFixed(2)}</b></div><Link onClick={()=>setOpenCart(false)} className="button" href="/checkout">Secure checkout</Link></>}</aside>}
