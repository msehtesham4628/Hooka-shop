"use client";import Link from "next/link";import {useStore} from "./store-provider";import type {Product} from "@/lib/catalog";
export function ProductCard({product}:{product:Product}){const {add,wishlist,toggleWish}=useStore();const price=product.salePrice??product.price;return <article className="card"><Link href={`/product/${product.slug}`}><div className="card-image"><img src={product.image} alt={product.name}/></div></Link>{product.salePrice&&<span className="badge">SAVE ${product.price-product.salePrice}</span>}<button className="wish" onClick={()=>toggleWish(product.id)} aria-label="Toggle wishlist">{wishlist.includes(product.id)?"♥":"♡"}</button><div className="card-info"><div className="eyebrow">{product.brand} · {product.weight}</div><Link href={`/product/${product.slug}`}><h3>{product.name}</h3></Link><div className="bottom"><span className="price">${price.toFixed(2)}{product.salePrice&&<del className="old">${product.price}</del>}</span><button onClick={()=>add(product)} disabled={!product.stock} className="add">{product.stock?"Add +":"Sold out"}</button></div></div><style jsx>{`h3{margin:6px 0 13px;font-size:15px}.wish{position:absolute;right:10px;top:10px;background:#0a0b0bbb;border:1px solid #ffffff22;border-radius:50%;width:32px;height:32px;color:#fff;cursor:pointer}.bottom{display:flex;justify-content:space-between;align-items:center}.add{font:700 10px Manrope;color:#a3ffca;background:none;border:0;cursor:pointer;text-transform:uppercase;letter-spacing:.08em}.add:disabled{color:#777}`}</style></article>}
